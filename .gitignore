# Secrets — never commit API keys
.env

# Python
__pycache__/
*.py[cod]
*.pyo
.Python
*.so
.venv/
venv/
env/

# OS / editor
.DS_Store
.idea/
.vscode/

# FAISS indexes are tracked so Streamlit Community Cloud (and clones) can run RAG without a build step.
# Rebuild locally with: python preprocess_to_faiss.py

# Large local datasets (uncomment if card.json is huge and should stay local)
# data/card.json
